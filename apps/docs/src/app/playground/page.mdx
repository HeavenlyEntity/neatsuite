export const metadata = {
  title: 'Playground',
  description: 'Interactive page to experiment with the NeatSuite HTTP client.',
}
import Playground from '@/components/Playground'
/* If your project has an alias like "@/components", you can do:
   import Playground from '@/components/Playground'
*/

# Interactive Playground

Type or edit code on the left and press **RUN** to see the result.

<Note>
  Learning demo only. Do **not** paste real credentials here.
</Note>

<Playground />
